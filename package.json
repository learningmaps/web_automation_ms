{
  "name": "web-automation-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for various web automation and scraping projects",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
